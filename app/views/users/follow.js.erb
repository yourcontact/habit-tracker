var searchUser = document.getElementById("user-search-<%= @user.id %>")

if (searchUser) {
  searchUser.outerHTML = "<%= j render 'users/user_item', user: @user %>"
}

var scoreboardUser = document.getElementById("user-scoreboard-<%= @user.id %>")

if (scoreboardUser) {
  scoreboardUser.remove()
}
